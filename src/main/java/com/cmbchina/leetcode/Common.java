package com.cmbchina.leetcode;
/**
 * java 常用操作
 *
 * 字符串
 * sb.length()
 * sb.append(str)
 * arr.length
 * sb.toString()
 * str.charAt(i)
 * double[] myList;         // 首选的方法
 * int[] arr = new int[10];
 * int[] arr = {1, 2, 5, 4}; //创建并初始化
 * for(int element: array){} //循环
 * set.add(key); //添加
 * set.contains(key); //是否包含
 * char []chars = s.toCharArray(); //字符串通常使用char数组处理
 * String.valueOf(chars); //char数组转换为字符串 很重要
 * sb.reverse(); //反转字符串
 * sb.append(c); //拼接字符串
 * res = Math.max(res, i-j+1); //最大值
 * for(Integer x: set2){new_nums[i++] = x;} //集合的遍历
 *
 *  PriorityQueue<Map.Entry<Integer, Integer>> maxheap = new PriorityQueue<>((a,b) -> b.getValue() - a.getValue()); //大根堆
 *
 * map.put(x, map.getOrDefault(x, 0)+1);
 *
 * //HashMap
 * put();
 * get();
 * getOrDefualt(key, 0);
 * containsKey(key);
 * containsValue(value);
 * size();
 * isEmpty();
 * clear();
 * remove();
 *
 * //HashSet
 *
 * add();
 * remove();
 * contains();
 * size();
 * isEmpty();
 *
 * //Queue
 * offer(); 插入
 * poll();  删除,返回队头元素
 * peek();  不删，返回队头元素
 *
 * //ArrayList
 * add();
 * get(index);
 * isEmpty();
 * size();
 * clear();
 * //排序
 * Collections.Sort(list, comparator<>)
 * Collections.reverse();
 *
 * */
public class Common {
}
